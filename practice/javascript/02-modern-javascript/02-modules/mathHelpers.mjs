/**
 * Title: Module helper functions
 * Learning objective: Export reusable logic from one file.
 */

export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}
